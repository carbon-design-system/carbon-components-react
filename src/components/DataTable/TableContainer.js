import wrapComponent from '../../tools/wrapComponent';

const TableContainer = wrapComponent({
  name: 'TableContainer',
  className: ['bx--data-table-v2-container'],
  type: 'div',
});

export default TableContainer;
